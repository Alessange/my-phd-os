import type { Command } from '@renderer/app/commands'

/** Palette commands for the calendar feature. The feature agent fills this in; the shell aggregates it. */
export const calendarCommands: Command[] = []
