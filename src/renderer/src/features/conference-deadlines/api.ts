// Query hooks and mutations for conference deadlines (TanStack Query over lib/api).
// Expected: useSubscriptions(), useAddSubscription(), useRemoveSubscription(), useRefreshNow(),
//   useRefreshStatus() (+ 'conferences:refreshStatus' push event), useConferenceDeadlines(filter),
//   useFollowedConferences(), useFollow()/useUnfollow(), useAddToCalendar(), useConferenceChanges().
// Keys: queryKeys.conferences.*
export {}
