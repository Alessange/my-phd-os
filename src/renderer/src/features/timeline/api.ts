// Query hooks and mutations for timeline milestones (TanStack Query over lib/api).
// Expected: useMilestones(), useMilestone(id), useCreateMilestone(), useUpdateMilestone(), useDeleteMilestone(),
//   useDismissedWarnings()/useDismissWarning() (settings:*Warning channels).
// Keys: queryKeys.milestones.*, queryKeys.settings.dismissedWarnings()
export {}
