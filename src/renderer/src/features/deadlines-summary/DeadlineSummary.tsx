/**
 * Compact summary above the Deadlines tabs (spec §14): active, due this week, at risk, completed,
 * nearest deadline, followed conferences — derived from real data only. Renders nothing until the
 * deadline features provide data; a fresh install must not show zero-valued cards.
 */
export function DeadlineSummary(): React.JSX.Element | null {
  return null
}
