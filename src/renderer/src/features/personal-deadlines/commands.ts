import type { Command } from '@renderer/app/commands'

/** Palette commands for the personal-deadlines feature. The feature agent fills this in; the shell aggregates it. */
export const personalDeadlineCommands: Command[] = []
