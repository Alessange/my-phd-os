// Query hooks and mutations for the Settings > Data section (TanStack Query over lib/api).
// Expected: useStorageInfo() -> data:getStorageInfo, useExportBackup(), useBackupImport() (preview -> commit),
//   useClearAllData(), useExportCalendar(scope). Settings themselves come from hooks/useSettings.
// Keys: queryKeys.data.*
export {}
