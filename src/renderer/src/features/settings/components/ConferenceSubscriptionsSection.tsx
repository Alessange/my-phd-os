import { SubscriptionManager } from '@renderer/features/conference-deadlines/components/SubscriptionManager'

/** Settings › Conference Subscriptions simply hosts the shared `SubscriptionManager`. */
export function ConferenceSubscriptionsSection(): React.JSX.Element {
  return <SubscriptionManager />
}
